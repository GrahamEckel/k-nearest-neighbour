A simple K-Nearest Neighbour implementation in Numpy.

To do:
- use a public dataset
- vectorize/generalize 
- expand:
    - use scipi BallTree and KDTree
    - use MNIST image data and classify images
